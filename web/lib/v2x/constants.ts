export const DEFAULT_LATITUDE = 12.918;
export const DEFAULT_LONGITUDE = 77.6205;

// Visual lift (in latitude degrees) used to arc V2V/V2I links on 2D/3D maps.
export const COMMUNICATION_ARC_LIFT = 0.00005;

// Decorative spin speed for the emergency mesh in the Three.js custom layer.
export const EMERGENCY_MESH_ROTATION_SPEED = 0.01;
